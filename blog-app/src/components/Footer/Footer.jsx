import React from 'react';
import { Box } from '@mui/material';

const Footer = () => {
    return (
        <Box display="none" component="footer">
            Footer
        </Box>
    );
};

export default Footer;
